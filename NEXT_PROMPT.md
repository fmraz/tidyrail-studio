# Tidyrail Studio - Continuation Prompt

Continue the existing project in `/Users/frantamraz/Documents/Software Company`.

Do not restart, rename Tidyrail Studio, replace Renewal Desk, rewrite Git history, or start a second product. Preserve unrelated founder work, especially the untracked file `NEXT_PROMPT 2.md`; never edit, stage, delete, or commit it.

Communicate with the founder only in Czech. Write all customer-facing UI, website copy, product documentation, policies, release notes, support text, store material, and marketing copy in clear English first.

## Mission and Product Standard

Tidyrail Studio builds focused, free, privacy-respecting tools that remove recurring effort, missed deadlines, scattered information, and avoidable stress. Renewal Desk must remain understandable in one sentence, account-optional, local-first, exportable, responsive, accessible, and honest about unavailable platforms.

Products are free for now. Do not add payments, subscriptions, Pro locks, artificial limits, advertising, invasive analytics, fake urgency, fake testimonials, fake users, fake metrics, or speculative monetization claims.

Prioritize user outcomes in this order:

1. Fix a broken public or release-candidate workflow.
2. Prevent data loss, privacy exposure, security failure, or inaccessible use.
3. Finish the closest missing Renewal Desk release gate.
4. Reduce steps, ambiguity, cognitive load, and error recovery cost.
5. Improve verified distribution and support.
6. Work on localization and approved account sync.
7. Start another product only after every Renewal Desk P0/P1 release gate is closed.

## Current Verified State - 2026-07-19

- Company: Tidyrail Studio.
- Product: Renewal Desk `0.1.0-mvp`.
- Domain: `tidyrailstudio.com`, registered through VEDOS.
- Repository: `fmraz/tidyrail-studio`.
- GitHub Pages source: `main` branch, `/docs`.
- Public HTTP homepage: `http://tidyrailstudio.com/`.
- Public web app: `http://tidyrailstudio.com/apps/renewal-desk/`.
- Public package: `http://tidyrailstudio.com/downloads/renewal-desk-0.1.0-mvp.zip`.
- Current ZIP SHA-256: `b8e99569946a23270e08c0f75fabb5baeb2583a9d75b5d9d9d3366edda1e7756`.
- `website/` and `docs/` are synchronized.
- Service-worker cache revision: `renewal-desk-0.1.10`; this is not the product version.
- Exactly one Tidyrail three-hour heartbeat automation is active. Keep duplicates paused.
- Repository work should be clean after the publication commit except founder-owned `NEXT_PROMPT 2.md`; verify before editing.

## Public Website - Implemented and Tested

- Original Tidyrail Studio Liquid Glass design inspired by modern iOS/macOS without copying Apple branding or assets.
- Responsive homepage, products, Renewal Desk product, downloads, about, support, contact, FAQ, roadmap, updates, press, privacy, security, terms, cookies, and custom 404 routes.
- Compact mobile navigation and touch-friendly controls.
- Lightweight nonblank first-frame canvas; full Three.js scene loads on first real scroll and replaces the fallback without errors.
- Reduced-motion visitors keep the static fallback.
- Privacy-friendly OS detection and manual download selection.
- Honest platform states: web and static ZIP available; native platforms are not publicly available.
- SEO/Open Graph metadata, sitemap, robots, CNAME, `.nojekyll`, referrer policy, local Three.js asset, and no invasive analytics.
- Account preview routes exist but remain out of navigation and blocked in robots.
- Custom 404 page and privacy-safe GitHub bug/feature request forms.
- Structured static audit passes 294 local references across 22 HTML files.
- Local Lighthouse scores:
  - Homepage: performance 94; accessibility, best practices, SEO 100.
  - Product: performance 97; accessibility, best practices, SEO 100.
  - Downloads: all four categories 100.
  - Renewal Desk app: all four categories 100.
- Full evidence is in `LIGHTHOUSE_REPORT.md`.

## Renewal Desk - Implemented and Tested

- Add, edit, delete, search, category filtering, dashboard summaries, timeline, calendar view, receipt status, and sample data.
- Monthly, quarterly, and yearly Renew action with eight-second Undo.
- Tested future, overdue, month-end, quarterly, yearly, and leap-day recurrence behavior.
- One-time and custom schedules stay manual by design.
- Local browser storage without required accounts or analytics.
- Versioned JSON backup plus legacy restore compatibility.
- Restore gates for app identity, version, dates, duplicates, item count, usable rows, and 5 MB file size.
- Replacement confirmation, destructive empty-backup warning, partial-import feedback, and immediate restore Undo.
- Spreadsheet CSV export and `.ics` calendar export.
- Network-first service worker with cached offline fallback, returning-user offline reload, and verified cache upgrade.
- Focus recovery after Add, Edit, Renew, Undo, Delete, and Clear All, including controls regenerated by rendering.
- Responsive 1280px and 390px layouts with no horizontal overflow; mobile dialog fits the viewport.
- No relevant console warnings or errors in the final browser QA.
- Deterministic recurrence, calendar, backup, sync-adapter, auth-config, packaging, and public-site tests pass.
- Current release notes: `products/renewal-desk/docs/RELEASE_NOTES_0.1.0_MVP.md`.

## Distribution State

- The static ZIP and checksum are synchronized in `dist/`, `website/downloads/`, and `docs/downloads/`.
- Archive integrity and all three checksums pass.
- Tauri desktop scaffold and icons are tracked.
- Current internal Apple Silicon DMG:
  - `desktop/renewal-desk/src-tauri/target/release/bundle/dmg/Renewal Desk_0.1.0_aarch64.dmg`
  - SHA-256: `98d55d03d02fec5726ee55205be78ddc89c0edd165d1a0755e4cf7759f12542f`
- The internal DMG passes the automated mount, bundle, drag-to-Applications, signature, and checksum QA.
- The DMG is ad-hoc signed, not notarized, ignored by Git, and must not be published as a public macOS download.
- Windows NSIS and Linux AppImage/deb metadata preflights pass, but real target-OS packages do not exist.
- iOS, Android, and widget apps do not exist.

## Confirmed Blockers and Causes

1. **HTTPS - P0 external blocker.** GitHub Pages reports `status: built`, `https_certificate.state: bad_authz`, and `https_enforced: false`. DNS apex A/AAAA, `www` CNAME, CNAME files, wildcard behavior, and CAA are correct. A successful Pages API custom-domain remove/re-add immediately reproduced `bad_authz`, proving repeated resets are not a useful next step. HTTP works; HTTPS serves a certificate without `tidyrailstudio.com`.
2. **Manual accessibility - P0 release gate.** Automated semantics, contrast, focus, responsive, and Lighthouse checks pass. A founder-controlled VoiceOver spot check is still required before calling the web release stable because browser automation is not an assistive-technology substitute.
3. **Accounts and sync - P1 gated.** No approved Supabase project/config exists and two real users have not passed RLS isolation. Valid enforced HTTPS is mandatory before exposing login, passwords, OAuth, or cloud data.
4. **macOS public package - P1 gated.** Full Xcode is not selected, no Developer ID Application identity exists, notarization credentials are absent, and clean-machine Gatekeeper QA has not passed.
5. **Windows/Linux packages - P1 gated.** Real builds require Windows/Linux target environments or reviewed CI runners and installation smoke tests.
6. **Localization - P1 incomplete.** The 31-language file is a scaffold, not full product/site localization. English source strings are still hardcoded and RTL/text-expansion QA is incomplete.
7. **Currency - P1 product limitation.** UI currently formats USD only. A migration and mixed-currency total policy must be designed before implementation.
8. **Mobile apps/widgets - P2 deferred.** Do not build them before the web data model, sync semantics, recovery, and localization architecture are stable.
9. **Legal drafts - P1 before accounts/stores.** Existing policies are drafts, not legal advice; update and review them when real account processing or store distribution begins.

## Hard Safety Rules

- Do not spend money, enter payment details, buy memberships, create paid accounts, submit stores, publish social posts, send email/support messages, or make irreversible external changes without explicit founder approval for that exact action.
- Do not commit passwords, personal registration data, tokens, OAuth secrets, service-role keys, `.env`, `auth-config.js`, signing identities, certificates, Apple API keys, or notarization credentials.
- Never expose a Supabase service-role key to browser code.
- Keep cloud sync disabled until HTTPS, approved config, schema constraints, two-user RLS, export/deletion, conflict handling, and recovery pass QA.
- Keep Enforce HTTPS disabled until `curl` validates a certificate containing `tidyrailstudio.com`.
- Do not publish or claim macOS, Windows, Linux, iOS, or Android availability until real artifacts pass platform-specific QA.
- Preserve local-first use, JSON backup, CSV/calendar export, and account-optional behavior.
- Keep technical terms such as RLS, cache revisions, signing internals, and checksums out of normal user UI.
- Never edit or commit founder-owned `NEXT_PROMPT 2.md`.

## Next Execution Order

### Phase 0 - Re-establish Evidence

1. Read `THREE_HOUR_WORK_LOOP.md`, this file, `IMPROVEMENT_BACKLOG.md`, `DECISION_LOG.md`, `QA_CHECKLIST.md`, and relevant product files.
2. Run `git status`, compare local/remote `main`, inspect the active automation, and verify no concurrent worker is editing the same tree.
3. Recheck GitHub Pages API, public HTTP, hostname-valid HTTPS, current GitHub Release assets, and package checksum before changing code.

### Phase 1 - Resolve HTTPS Through Support

1. Recheck HTTPS once. If it is valid, enable Enforce HTTPS and verify HTTP-to-HTTPS redirect plus all critical routes.
2. If `bad_authz` remains, do not loop remove/re-add resets.
3. Present `GITHUB_SUPPORT_HTTPS_REQUEST.md` to the founder for exact approval and manual support submission while signed in as `fmraz`.
4. Record the ticket reference after the founder supplies it.
5. Keep account links, authentication, OAuth, sync, and password forms hidden/disabled until valid enforced HTTPS is proven.

### Phase 2 - Manual VoiceOver Release Gate

1. Prepare a short Czech founder checklist for VoiceOver on macOS: dashboard landmarks, Add dialog entry/close, item actions, Renew/Undo status, export status, and mobile menu.
2. Do not claim that automated DOM checks replace VoiceOver.
3. Record exact results and defects in `products/renewal-desk/docs/QA_REPORT.md` and `QA_CHECKLIST.md`.
4. Fix any discovered focus, label, order, live-region, or keyboard trap defect; repeat browser and deterministic regression tests.

### Phase 3 - Stable Web Release Gate

1. After HTTPS and VoiceOver pass, decide whether to promote the local-first web release to `0.9.0` or `1.0.0` using `VERSIONING.md`; do not bump merely for appearance.
2. Rebuild ZIP/checksum copies after any packaged change.
3. Re-run Lighthouse, structured link audit, 1280px/390px browser QA, offline reload, service-worker upgrade, archive integrity, and public URL checks.
4. Update release notes, changelog, package hashes, GitHub Release assets, and public download links together.

### Phase 4 - Safe Accounts and Cloud Sync

Start only after valid enforced HTTPS and explicit founder approval to create/use a Supabase project.

1. Follow `SUPABASE_SETUP.md`; use ignored local config and environment variables only.
2. Review `DATABASE_SCHEMA.md`; add constraints for allowed categories/cycles/receipt states, field lengths, date validity, currency format, and sane amount ranges.
3. Run two-user RLS QA: anonymous blocked; A cannot read/write/delete B; B cannot read/write/delete A; wrong-owner inserts fail; cleanup succeeds.
4. Test registration, verification, login, logout, reset, session expiry, rate-limit errors, account export, and deletion request.
5. Implement first sync as an explicit, conflict-safe migration with backup and recovery. Never silently overwrite local or cloud records.
6. Keep account use optional and local mode fully functional.
7. Update policies and support material before adding account links to navigation or sitemap.

### Phase 5 - Localization and Currency

1. Inventory every user-facing website and app string.
2. Build one English source catalog with stable keys, interpolation, pluralization, date, and number rules.
3. Implement English and Czech first; QA all routes and app states at desktop and 390px.
4. Add Slovak, German, Polish, French, Spanish, Italian, Portuguese, and Dutch in reviewed batches; then continue the remaining planned locales.
5. Test Arabic/Hebrew RTL, CJK line breaking, long German/Finnish text, validation messages, and manual language preference persistence.
6. Define currency behavior before coding. Never sum different currencies into one misleading annual total.

### Phase 6 - Native Distribution

1. Keep the current DMG internal-only.
2. Treat full Xcode installation/selection, Apple Developer membership, Developer ID certificates, notarization credentials, Windows signing, and any fees as founder-only approval steps.
3. Build, Developer-ID sign, notarize, staple, and clean-machine test macOS before publishing.
4. Build Windows on Windows/reviewed CI and test install, launch, backup/restore, uninstall, and SmartScreen behavior.
5. Build Linux on Linux/reviewed CI and test AppImage/deb launch, icons, permissions, desktop entry, and uninstall guidance.
6. Publish only verified artifacts with checksums, release notes, install guides, and honest availability labels.

### Phase 7 - Mobile and Widgets

Start only after web sync, recovery, and localization are stable.

1. Evaluate Capacitor versus Expo/React Native on code reuse, native UX, offline data, auth redirects, widgets, maintenance, and store compliance.
2. Build the smallest useful iOS/Android shell with native navigation, local cache, login, export, and sync recovery.
3. Add widgets only for immediate value such as next deadline, overdue count, or quick add.
4. Complete simulator/device accessibility and store privacy QA before submission.
5. Never submit without explicit founder approval and required memberships/identity steps.

## Required Verification After Every Change

- Test the exact changed flow at desktop and 390px mobile.
- Check console, overflow, clipping, empty/error/offline/reduced-motion states, visible focus, accessible names, and live status.
- Run relevant deterministic scripts and `scripts/qa-public-site.py`.
- Keep `products/renewal-desk/`, `website/apps/renewal-desk/`, and `docs/apps/renewal-desk/` synchronized.
- Keep `website/` and `docs/` identical.
- Rebuild all ZIP/checksum copies after packaged changes.
- Keep generated `target/`, credentials, local auth config, and `NEXT_PROMPT 2.md` out of Git.
- Run `git diff --check`, inspect staged scope, commit intentionally, push, wait for Pages, and verify public files.
- Update this prompt at the end with evidence, not assumptions.

## Immediate Next Primary Outcome

Choose exactly one: **obtain a GitHub Support ticket/reference for the stuck Pages ACME authorization, or prove hostname-valid HTTPS and enable Enforce HTTPS if GitHub has fixed it**.

If founder action is unavailable and HTTPS remains externally blocked, use the cycle for the VoiceOver checklist/manual gate preparation or English/Czech string inventory. Do not start another product, enable Supabase, or publish the unsigned DMG.

## Required Czech End Report

Report concisely:

1. Co bylo dokončeno.
2. Jaký uživatelský přínos vznikl.
3. Aktuální stav webu a Renewal Desk.
4. HTTPS, GitHub Pages a distribuce.
5. Změněné soubory, commit a veřejné URL.
6. Testy a měřitelné důkazy.
7. Zbývající problémy, jejich příčina a riziko.
8. Co vyžaduje founderův účet, identitu, platbu nebo ruční zásah.
9. Další jediný nejlepší krok.
10. Aktualizovaný `NEXT_PROMPT.md`.
